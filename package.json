{
  "name": "root",
  "private": true,
  "workspaces": [
    "day_2_operations",
    "mgmt_ui",
    "common_components",
    "DMSystem"
  ],
  "scripts": {
    "clear-ports": "node -e \"[3003,3001,4000,3004].forEach(port => { console.log(`Clearing port ${port}...`); require('child_process').execSync(`npx kill-port ${port}`, { stdio: 'inherit' }) })\"",
    "start": "npm run clear-ports && lerna run --parallel start",
    "build": "lerna run --parallel build"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.9"
  }
}
