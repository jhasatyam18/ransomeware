import React from 'react';
// import { Link } from 'react-router-dom';

const AlertActionRenderer: React.FC<any> = () => {
    const path = '#';
    return (
        <>
            <a href={path}>Fix This</a>
        </>
    );
};

export default AlertActionRenderer;
